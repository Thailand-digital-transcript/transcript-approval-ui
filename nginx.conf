server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes (/queue, /batches/:id, /monitor) must
    # serve index.html so react-router can resolve them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
